# Basic security & URL handling
Options -Indexes
RewriteEngine On

# If you later want to route everything through public/, you can uncomment:
# RewriteCond %{REQUEST_URI} !^/public/
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)$ public/$1 [L]
